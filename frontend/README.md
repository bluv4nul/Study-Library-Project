# ElectroLibrary Frontend

## Описание

Frontend-часть проекта **ElectroLibrary**, реализованная с использованием **Vue 3**, **Vue Router** и **Axios**.

Приложение предоставляет удобный пользовательский интерфейс для взаимодействия с серверной частью электронной библиотеки и позволяет выполнять основные операции управления коллекцией книг.

Дизайн интерфейса выполнен в минималистичном стиле.

---

## Функциональные возможности

### Работа с книгами

* Просмотр списка книг.
* Просмотр подробной информации о книге.
* Создание новых записей.
* Редактирование существующих книг.
* Удаление книг из коллекции.

### Поиск и фильтрация

* Поиск книг по названию.
* Фильтрация по автору.
* Фильтрация по издательству.
* Фильтрация по жанру.
* Сортировка списка книг по различным параметрам.

### Навигация

Для организации многостраничного интерфейса используется Vue Router.

Доступные маршруты:

| Маршрут      | Описание                       |
| ------------ | ------------------------------ |
| `/`          | Перенаправление на список книг |
| `/books`     | Список всех книг               |
| `/books/:id` | Подробная информация о книге   |
| `/about`     | Информация о проекте           |

---

## Используемые технологии

### Основные технологии

* Vue 3
* Vue Router
* Axios
* Vite

### Вспомогательные технологии

* HTML5
* CSS3
* JavaScript ES2022

### Взаимодействие с сервером

Frontend взаимодействует с REST API серверной части посредством HTTP-запросов.

Основные методы API:

| Метод  | Назначение        |
| ------ | ----------------- |
| GET    | Получение данных  |
| POST   | Создание записи   |
| PATCH  | Обновление записи |
| DELETE | Удаление записи   |

---

## Структура проекта

```text
frontend/
│
├── public/
│
├── src/
│   ├── api/
│   │   └── libraryApi.js
│   │
│   ├── components/
│   │   ├── AppButton.vue
│   │   ├── BaseModal.vue
│   │   ├── BookCard.vue
│   │   ├── BookForm.vue
│   │   └── PageHeader.vue
│   │
│   ├── pages/
│   │   ├── BooksPage.vue
│   │   ├── BookDetailsPage.vue
│   │   └── AboutPage.vue
│   │
│   ├── router/
│   │   └── index.js
│   │
│   ├── App.vue
│   ├── main.js
│   └── style.css
│
├── Dockerfile
├── package.json
└── vite.config.js
```

---

## Компонентная архитектура

### BookCard

Компонент отображения информации о книге в виде карточки.

Функции:

* отображение названия книги;
* отображение автора;
* отображение года издания;
* переход к подробной информации.

### BookForm

Компонент формы создания и редактирования книги.

Содержит поля:

* название;
* автор;
* издательство;
* жанр;
* год издания;
* описание.

### BaseModal

Универсальное модальное окно.

Использует механизм слотов Vue для передачи содержимого.

### PageHeader

Переиспользуемый компонент заголовка страницы.

---

## Использование слотов

В проекте используются слоты Vue для повышения гибкости компонентов.

Пример:

```vue
<BaseModal>
  <template #default>
    <BookForm />
  </template>
</BaseModal>
```

Использование слотов позволяет переиспользовать модальные окна и другие компоненты для различных задач.

---

## Установка и запуск

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

После запуска приложение будет доступно по адресу:

```text
http://localhost:5173
```

---

## Сборка проекта

Создание production-сборки:

```bash
npm run build
```

Предпросмотр production-версии:

```bash
npm run preview
```

---

## Docker

Для контейнеризации используется Docker.

Сборка образа:

```bash
docker build -t electrolibrary-frontend .
```

Запуск контейнера:

```bash
docker run -p 5173:80 electrolibrary-frontend
```

После запуска приложение будет доступно по адресу:

```text
http://localhost:5173
```

---

## Интеграция с Backend

Frontend подключается к серверной части через REST API.

Базовый адрес API:

```text
http://localhost:3000/api
```

Для взаимодействия используется библиотека Axios и отдельный слой API-запросов (`libraryApi.js`).

---

## Результат

В результате была разработана клиентская часть приложения ElectroLibrary, обеспечивающая:

* удобное управление библиотекой книг;
* современный пользовательский интерфейс;
* взаимодействие с REST API;
* маршрутизацию между страницами;
* компонентный подход Vue 3;
* использование слотов;
* возможность запуска через Docker.
